# Assignment 1
contains a data cleaning project performed on the 'xAPI-Edu-Dataset.csv' dataset. It includes tasks like handling missing values, removing duplicates, fixing data inconsistencies, and converting data types. The cleaned dataset, 'cleaned_dataset.csv' is available for further analysis.

# Assignment 2
contains two data cleaning projects performed on the 'customerdemographic.csv' and 'transactions.csv' dataset. It includes tasks like handling missing values, removing duplicates, fixing data inconsistencies, and converting data types. The cleaned dataset, 'cleaned_customerdemographic.csv' and cleaned_transactions.csv are available for further analysis.
